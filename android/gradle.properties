android.useAndroidX=true

# Increase memory for Gradle
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# NOTE: this project builds with JDK 17. If your system default differs, set
# org.gradle.java.home in a LOCAL, untracked gradle.properties (e.g. ~/.gradle/)
# or use Android Studio's bundled JBR — do not commit a machine-specific path here.
